# 如何把工具注册进「开发工具箱」

完整说明见同目录 **AGENTS.md**（GitHub 仓库、安装目录、`http://127.0.0.1:17890/AGENTS.md` 三处内容相同）。

最短路径：

1. 在项目根写 `.devtoolbox.json`
2. `devtoolbox register --file .devtoolbox.json`
3. 不要再往桌面放单独快捷方式

关闭后台：`devtoolbox stop <id>` 或界面上的「关闭」。
支援系统：macOS + Windows。
模板：`examples/` 。


最短路径：

1. 在项目根写 `.devtoolbox.json`
2. `devtoolbox register --file .devtoolbox.json`
3. 不要再往桌面放单独快捷方式

关闭后台：`devtoolbox stop <id>` 或界面上的「关闭」。
支援系统：macOS + Windows。
