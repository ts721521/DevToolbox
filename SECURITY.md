# Security Policy

本项目是本机 loopback 工具启动器，不处理云端账号密码。仍须避免把本机路径、凭据写进公开仓库。

## 报告漏洞

**不要**用公开 GitHub Issue 报告安全问题。请通过 GitHub Security Advisory 或私下联系仓库 owner。

期望 72 小时内确认收到。

## 支持版本

| 版本 | 支持状态 |
|------|---------|
| `main` 最新 | 接受安全修复 |
| 最近一个 release tag | 接受安全修复 |
| 更早版本 | 请升级 |

## 红线

- 禁止把真实路径、token、cookie、密码提交进 git
- 服务默认只绑 `127.0.0.1`
- 注册 JSON 里不要写密钥；需要时用环境变量并由工具自己读取
